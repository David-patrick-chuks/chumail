export const CHUNK_CONFIG = {
    MAX_LENGTH: 2000,
    OVERLAP: 400,
};

export const SUPPORTED_FILE_TYPES = {
    DOCUMENTS: ['.txt', '.pdf', '.docx', '.doc', '.csv'],
    AUDIO: ['.mp3', '.wav', '.m4a', '.ogg'],
    VIDEO: ['.mp4', '.mov'],
};

export const EMBEDDING_DIMENSIONS = 768;

export const DEFAULT_MATCH_THRESHOLD = 0.5;

